var welcomePrompt = alert("Welcome to your ATM.");


var startBalance = prompt("How much money is in your account?");
  if (startBalance != null && startBalance != '')
    {
        document.write("Your starting balance is " + startBalance);
    }
