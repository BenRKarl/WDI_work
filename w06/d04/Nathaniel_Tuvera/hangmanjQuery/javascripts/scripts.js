var answerArray = ['acetone', 'glycerin', 'monarch', 'pliers', 'curtains', 'monitor', 'lampshade', 'secret', 'speakers'];

function randomAnswer(){
  var answerId = Math.floor((Math.random()*answerArray.length));
  var answer   = answerArray[answerId];

  return answer;
}

function guess(letter){
  if(letter = )
}
