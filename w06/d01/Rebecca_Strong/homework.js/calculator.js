var calculator = function (number) {
    var valMult = number * number;
    var valAdd = number + number;
    var valSub = number - number;
    var valDiv = number / number;
    console.log(valMult, valAdd, valSub, valDiv);
  };

