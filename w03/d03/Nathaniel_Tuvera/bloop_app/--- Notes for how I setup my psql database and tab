--- Notes for how I setup my psql database and tables ---

CREATE DATABASE wdi_bloop_app;

\c wdi_bloop_app

CREATE TABLE users (
  id serial4 PRIMARY KEY,
  username varchar(255)
  );

  CREATE TABLE bloops (
  id serial4 PRIMARY KEY,
  message varchar(255),
  user_id integer
  );
