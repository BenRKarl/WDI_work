
attr_accessor = :hearing

class Grandma

def initialize
@ deaf = true
end

def listen ("")
  if user_input.chomp == input.chomp.downcase
    puts "WHAT's THAT? COME AGAIN, SONNY!"
  else user_input.chomp == user_input.chomp.upcase
    puts "NO, NOT SINCE 1929!"
    end

def .loud_enough? (input)
  if user_input_arr = input.gsub (" ","").split("") #to generate an array. Also "string".chars eg "string".split("ri")
  upcase_arr = input_arr.select{|char| char == char.upcase}
  upcase_arr.length.to_f / input_arr. > 0.5
  #above line is only line necessary bcs the return is 50% /n so works for downcase testing as well.

  downcase_arr = input



end
